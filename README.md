# rockpaperscissor
ock Paper Scissor is a simple command-line game built using Python where the user plays against the computer. It uses random selection and conditional logic to determine the winner based on classic game rules.
